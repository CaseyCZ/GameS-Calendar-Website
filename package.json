{
  "name": "games-calendar-website",
  "version": "2.2.0",
  "private": true,
  "description": "Rychlý herní kalendář s termíny vydání a bezplatným obohacením metadat z Wikidata a Wikipedie",
  "scripts": {
    "fetch-games": "node fetch-games.js",
    "check": "node --check fetch-games.js && node --check js/data.js && node --check js/calendar.js && node --check js/ui.js && node --check js/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
